%h1.fs-2= @lesson.name

.ms-auto
  .mt-2 質問の内容: #{@lesson_question.content}

.mt-5
  = link_to '編集', edit_admins_lesson_lesson_question_path(@lesson, @lesson_question), class: 'btn btn-primary'
  = button_to '削除', admins_lesson_lesson_question_path(@lesson, @lesson_question), method: :delete, data: { turbo_confirm: t('views.common.confirm.destroy') }, class: 'btn btn-danger'
