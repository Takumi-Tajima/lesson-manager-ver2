%table.table.mt-3
  %thead
    %tr
      %th ID
      %th 開始日時
      %th 終了日時
      %th 予約人数
      %th 募集人数

  %tbody.align-middle
    - lesson_dates.each do |lesson_date|
      %tr
        %td= link_to lesson_date.id, admins_lesson_lesson_date_path(lesson_date.lesson, lesson_date)
        %td= l(lesson_date.start_at)
        %td= l(lesson_date.end_at)
        %td= lesson_date.reservations_count
        %td= lesson_date.capacity
